import React from 'react'
import { buildImageObj } from '../../lib/helpers'
import { imageUrlFor } from '../../lib/image-url'



function Faq (props) {
  return (
    <faq className={styles.root}>
    </faq>
  )
}

export default Faq
